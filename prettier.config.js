const config = {
  semi: false,
  singleQuote: false,
  trailingComma: "all",
  endOfLine: "lf",
  plugins: [
    "prettier-plugin-tailwindcss", // must be registered last
  ],
}

export default config
